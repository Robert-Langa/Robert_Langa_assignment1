// Displayable enforces a common interface for printing object information.
// Any class implementing this must provide its own displayInfo() logic.
abstract class Displayable {
  void displayInfo();
}
